<!-- snippet/product-price.liquid -->
{% if variant.title %}
    {%- assign compare_at_price = variant.compare_at_price -%}
    {%- assign price = variant.price -%}
    {%- assign available = variant.available -%}
{% else %}
    {%- assign compare_at_price = 1999 -%}
    {%- assign price = 1999 -%}
    {%- assign available = true -%}
{% endif %}

<div class="product-price  {% if compare_at_price > price %}price--on-sale{% endif %} " data-price style="">
    {% if compare_at_price > price %}
      MRP:
        <span class="price-item price-item--regular" data-regular-price>
          {{ compare_at_price | money }}
        </span>
  <br>
        <span class="price-item price-item--sale" data-sale-price>
            OFFER PRICE:{{ price | money }}
        </span>
 
    {% else %}
       
        <span class="price-item price-item--regular" data-regular-price>
            {{ price | money }}
        </span>
        <span class="price-item price-item--sale" data-sale-price></span>
  
   <span class="sale savings" style="background: #dfa649;padding: 7px 8px;font-weight: bolder;">
                  {% if variant.price < variant.compare_at_price and variant.available %}
                  <span style="{% if collection.title contains 'The Big Mega Sale' %}color: #000;font-weight: bold;{% endif %}">{{ variant.compare_at_price | minus: variant.price | times: 100 | divided_by: variant.compare_at_price }}% off</span>
                  {% endif %}
                </span>
    {% endif %}
  
     {% if settings.show_wishlist %}
        <a class="wishlist {% if settings.show_wishlist_mb %}wishlist_mb{% endif %}" aria-label="wishlist" data-icon-wishlist href="#" data-product-handle="{{ product.handle }}" data-id="{{ product.id }}">
            {% render 'icon-heart' %}
        </a>
    {% endif %}
  
</div>

<div class="product-price  {% if compare_at_price > price %}price--on-sale{% endif %} " style="margin-bottom: 15px;margin-top: 10px;">
    {% if compare_at_price > price %}
     {% if template != 'product'  %} 
   <span class="sale savings"  style="background: #f3f3f3;padding: 7px 8px;font-weight: bolder;">
                 
                  You Save  <span style="{% if collection.title contains 'The Big Mega Sale' %}color: #000;font-weight: bold;{% endif %}">{{ variant.compare_at_price | minus: variant.price | times: 100 | divided_by: variant.compare_at_price }}% </span> (<span class="money">{{ variant.compare_at_price | minus: variant.price | money }}</span>)
                
                </span>
  
  {% endif %}
  {% if template == 'product' %}
     <span class="sale savings"  {% if product.available %}style="background: #f3f3f3;padding: 7px 8px;font-weight: bolder;"{% endif %}>
                  {% if variant.price < variant.compare_at_price and variant.available %}
                  You Save  <span style="{% if collection.title contains 'The Big Mega Sale' %}color: #000;font-weight: bold;{% endif %}">{{ variant.compare_at_price | minus: variant.price | times: 100 | divided_by: variant.compare_at_price }}% </span> (<span class="money">{{ variant.compare_at_price | minus: variant.price | money }}</span>)
                  {% endif %}
                </span>
   &nbsp;(inclusive of all taxes)
  
  {% endif %}
   
    {% endif %}
  
    
</div>