<ul class="site-nav-dropdown">
    {% for link_child_item in linklists[link_child].links %}
    <li class="menu-lv-2{% if linklists[link_child_item.handle] != empty %} dropdown{% endif %}">
        <p class="nav-action{% if linklists[link_child_item.handle] != empty %} menu__moblie{% else %} menu__moblie_end{% endif %}">
            <a href="{{ link_child_item.url }}" class="link">{{ link_child_item.title }}</a>
            {% if linklists[link_child_item.handle] != empty %}
                <span class="icon-dropdown" data-toggle-menu-mb>&#10095;</span>
            {% endif %}
        </p>
        
        {% if linklists[link_child_item.handle] != empty %}
        
        <div class="sub-menu-mobile">
            <div class="menu-mb-title">
              
                <span class="icon-dropdown">&#10094;</span>
                 <span class="icon-dropdown" data-toggle-menu-mb>&#10095;</span>
                {{ link_child_item.title }}
            </div>
            <ul class="site-nav-dropdown">
                {% for n in linklists[link_child_item.handle].links %}                
                <li class="menu-lv-3">
                    <p class="nav-action menu__moblie_end">
                        <a href="{{ n.url }}" class="link">
                            {{ n.title }}
                        </a>
                      {% if linklists[link_child_item.handle] != empty %}
                <span class="icon-dropdown" data-toggle-menu-mb>&#10095;</span>
            {% endif %}
                    </p>
                </li>
                {% endfor %}
            </ul>
        </div>
        {% endif %}
    </li>
    {% endfor %}
</ul>